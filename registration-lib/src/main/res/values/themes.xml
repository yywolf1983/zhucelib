<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 常规主题 -->
    <style name="Theme.RegGate" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/reggate_primary</item>
        <item name="colorPrimaryDark">@color/reggate_primary_dark</item>
        <item name="colorAccent">@color/reggate_accent</item>
    </style>

    <!-- 透明主题:用于 RegistrationGateActivity 路由期间 -->
    <style name="Theme.RegGate.Transparent" parent="Theme.RegGate">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- 对话框主题:用于 TrialDialogActivity -->
    <style name="Theme.RegGate.Dialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorPrimary">@color/reggate_primary</item>
        <item name="colorAccent">@color/reggate_accent</item>
        <item name="android:windowMinWidthMajor">85%</item>
        <item name="android:windowMinWidthMinor">85%</item>
    </style>

</resources>
