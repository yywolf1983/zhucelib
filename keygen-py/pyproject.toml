[project]
name = "regate-keygen"
version = "0.1.0"
description = "RegGate 注册机（Python GUI，基于 Tkinter）"
requires-python = ">=3.10"
dependencies = [
    "cryptography>=42.0",
]

[tool.uv]
package = false
